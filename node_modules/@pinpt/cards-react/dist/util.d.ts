import { ENVIRONMENTS } from './types';
export declare const getCardEmbedUrl: (environment: ENVIRONMENTS, profile_id: string, editingEnabled: boolean, fullScreen: boolean, disableChrome: boolean, frameless: boolean) => string;
export declare const getMockCardEmbedUrl: (avatar: string, firstName: string, lastName: string) => string;
